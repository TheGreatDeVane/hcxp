/ Set page title
- @title = "Browse events"

.result-scopes
  ul.nav.nav-tabs.no-margin
    = time_scope_filter_link(@q, t('future_events'), :future)
    = time_scope_filter_link(@q, t('past_events'), :past)
  .clearfix

= render 'events/events', event_months: @event_months, events: @events

- content_for :sidebar do

  .panel.panel-default
    .panel-body
      = search_form_for @q, url: browse_events_path do |f|

        .form-group.hidden
          h6.form-label Events from
          label.radio-inline
            = f.radio_button :from_the, 'future'
            = t('future')

          label.radio-inline
            = f.radio_button :from_the, 'past'
            = t('past')

        .form-group
          h6.form-label = t('band')
          = f.search_field :bands_name_cont, class: 'form-control input-sm'

        .form-group
          h6.form-label = t('location')
          = f.search_field :venue_address_cont, class: 'form-control input-sm'

        = f.submit t('search'), class: 'btn btn-default btn-sm btn-block'

  coffee:
    #$(document).on 'change', '#q_from_the_future', (e) ->
    #  $('#event_search').submit()

    #$(document).on 'change', '#q_from_the_past', (e) ->
    #  $('#event_search').submit()

    $(document).on 'submit', '#event_search', (e) ->
      #$.pjax.submit e, '.pjax-container'